# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: dart-lang/setup-dart@v1
      # - run: ./tool/get-deps.sh
      - run: git submodule update --init --recursive
      - run: curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.38.0/install.sh | bash
      - run: \curl -sSL https://get.rvm.io | bash -s stable
      - run: echo "export PATH=\"$PATH:$HOME/.rvm/bin\"" >> $HOME/.bashrc
      - run: source ./tool/env-set.sh
      - run: ./tool/before-install.sh && ./tool/install.sh && ./tool/build.sh
      - run: ./tool/shared/deploy.sh
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ANGULARDART_COMMUNITY }}'
          channelId: live
          projectId: angulardart-community