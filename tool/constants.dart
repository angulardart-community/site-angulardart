import 'dart:io';

String fragPath = 'tmp/_fragments';
String srcPath = 'src';
Directory builtExamplesDir = Directory('tmp/deploy-repos/examples');
// TODO: switch main version to 6 (or read pubspec) once syncing is done
int angularVersion = 5;
